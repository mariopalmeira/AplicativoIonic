//Quando se trata de uma classe de domínio no frontend
//usa-se uma interface e dai a nomenclatura fica como no backend
//a não ser pelas particularidades do typescript
export interface CategoriaDTO{
    id:string;
    nome:string;
}